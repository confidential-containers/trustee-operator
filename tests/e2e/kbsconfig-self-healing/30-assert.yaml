apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 120
commands:
  # KbsConfig must be fully deleted — not stuck in Terminating state.
  - script: |
      kubectl get kbsconfig kbsconfig-sample -n trustee-operator-system 2>&1 | grep -q "not found"
