## TrusteeConfig samples - Recommended approach ##
# TrusteeConfig automatically generates all required ConfigMaps and Secrets
# for KBS deployment, simplifying the configuration process.

namespace: trustee-operator-system

resources:
# Basic permissive configuration (recommended for development/testing)
- trusteeconfig-basic.yaml

# Uncomment for production-ready restricted configuration
# - trusteeconfig-restricted.yaml

# Platform-specific examples (IBM SE)
# Uncomment if you need IBM Secure Execution support
# - ibmse-pv.yaml
# - ibmse-pvc.yaml

# Legacy KbsConfig approach (deprecated - for reference only)
# See the legacy/ directory for old-style manual ConfigMap/Secret samples
# - kbsconfig_sample.yaml

#+kubebuilder:scaffold:manifestskustomizesamples
